# 使用 npm 镜像源全局安装 bun
npm install -g bun --registry=https://registry.npmmirror.com

# 1. 进入前端 web 目录
cd web

# 2. 彻底清理 node_modules（非常重要，防止 npm 的残留干扰 bun）
rm -r -Force node_modules

# 3. 使用绝对路径运行 bun 安装
& "D:\RJ\node\node_global\bun" install

# 4. 启动开发服务器
& "D:\RJ\node\node_global\bun" run dev

# 5.后端启动
go run main.go

# 1.注意 合并官方代码需要先提交自己代码不然容易出现问题

## 1. 基础概念
在双仓库开发模式中，我们通常定义两个远程：
* **origin**: 你自己的仓库地址（拥有读写权限，用于保存个人代码）。
* **upstream**: 官方/原作者的仓库地址（通常只有读权限，用于同步最新更新）。

### 第一步：检查现有远程仓库
首先，进入你的项目根目录，查看当前的远程仓库配置：
git remote -v

### 第二步：重新命名现有的 origin（可选）
如果现在的 origin 指向的是官方仓库，建议先把它改名为 upstream：
git remote rename origin upstream

### 第三步：添加你的个人仓库作为 origin
现在，将你在 GitHub/Gitee 上创建的私有仓库地址添加进来：
# 替换为你的实际仓库地址
git remote add origin https://github.com/你的用户名/你的仓库名.git

### 第四步：手动创建 upstream（如果你已经有 origin）
如果你现在的 origin 已经是你自己的仓库，只需直接添加官方仓库即可：
# 替换为官方仓库的原始地址
git remote add upstream https://github.com/QuantumNous/new-api.git

### 第五步：验证配置
再次运行查看命令，确认输出如下结构：
git remote -v

预期输出：
origin    https://github.com/你的用户名/你的仓库名.git (fetch)
origin    https://github.com/你的用户名/你的仓库名.git (push)
upstream  https://github.com/QuantumNous/new-api.git (fetch)
upstream  https://github.com/QuantumNous/new-api.git (push)

## 3. 常用同步工作流
配置完成后，你可以使用以下命令组合来保持代码最新：

### 1. 同步官方代码到本地
# 获取官方更新
git fetch upstream

# 合并官方主分支到本地
git merge upstream/main

### 2. 将本地所有代码（含官方更新+个人开发）推送到自己仓库
# 推送至你自己的 origin 仓库
git push origin main

# 获取官方最新的远程分支信息
git fetch upstream

# 将官方的 main 分支合并到你当前的本地分支
git merge upstream/main
